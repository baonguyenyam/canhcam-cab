function CCFooter13() {
    // $('.canhcam-footer-13 .validator').validator().on('submit', function(e) {
    //     if (e.isDefaultPrevented()) {
    //         alert($(this).find('input').attr('data-error'))
    //     } else {
    //         $(this)[0].reset();
    //     }
    //     return false
    // })
};

$(function() {
    CCFooter13();
})

$(window).resize(function() {})
